@model List<RoboDex__Capstone_.Models.RoboDexer>

@{
    ViewData["Title"] = "Followers";
}

<h1>Followers</h1>

<div>
    
    <hr />
    <dl class="row">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model[0].UserName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model[0].IMGUrl)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model[0].ContactInfo)
                    </th>


                </tr>
            </thead>

            <tbody>
                @for (int i = 0; i < Model.Count; i++)
                {
                    @*<div class="row">
                        <div class="col-4">
                            <img src="@Html.ViewData.Model[i].IMGUrl" style="height: 350px; width: 350px;" />
                        </div>

                    </div>*@
                    <tr>
                        <td>
                            @Html.DisplayFor(model => model[i].UserName)
                        </td>
                        <td>
                            @Html.DisplayFor(model => model[i].IMGUrl)
                        </td>
                        <td>
                            @Html.DisplayFor(model => model[i].ContactInfo)
                        </td>

                        @*<td>
                    <a asp-action="ItemDetails" asp-route-id="@ViewData.Model[i].Item.ItemId">Details</a>
                </td>*@

                    </tr>
                }
</div>
<div>
    @*<a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>*@
</div>
