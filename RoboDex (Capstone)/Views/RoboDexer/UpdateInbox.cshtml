@model List<RoboDex__Capstone_.Models.Inbox>

@{
    ViewData["Title"] = "UpdateInbox";
}

<h1>UpdateInbox</h1>

<div>
    <h4>Inbox</h4>
    <hr />
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model[0].Message)
                </th>

        </thead>

        <tbody>
            @*@foreach (var item in Model)*@
            @for (int i = 0; i < Model.Count; i++)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => modelItem[i].Message)
                </td>
                <td>
                    <a asp-action="ItemDetails" asp-route-id="@Model[i].ItemId">Details</a>
                    <a asp-action="MessageRead" asp-route-id="@Model[i].Id">Mark</a>
                </td>

                            @*<td>
                    <div class="checkbox">
                        <label>
                            <input type="checkbox"
                                    name="MessageRead"
                                    value="@Model[i].isRead"
                                    @if (Model[i].isRead == false) { <text> checked </text> } /> @Model[i].isRead
                            </label>
                        </div>
                    </td>*@
                
            </tr>
                
            }
        </tbody>
    </table>
    
</div>
<div>
    <div class="form-group text-center">
        <input type="submit" class="btn btn-primary" value="Submit" />
    </div>
    @*<a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |*@
    <a asp-action="Index">Back to List</a>
</div>
